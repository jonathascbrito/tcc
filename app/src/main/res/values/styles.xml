<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryGestrans</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkGestrans</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/windowBackgroundGestrans</item>
        <item name="windowActionBar">false</item>
    </style>


</resources>
